#!/usr/bin/env bash
# This login's secrets, from Doppler into the places the tools read them
# (sync) or the state of that (status). A thin exec of
# runtime/provisioning/secrets/fabric-secrets; prints no secret value.
exec "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")/../runtime/provisioning/secrets/fabric-secrets" "$@"
