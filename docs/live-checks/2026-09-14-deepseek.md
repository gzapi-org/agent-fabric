# Live check 2026-09-14 — DeepSeek V4 through the broker path

What was actually served, read back from OpenRouter's `/api/v1/generation?id=`
records; generation ids are the `requestId` values in the Claude Code
transcripts of the sessions named. Host `develop-qzapp`, agent `user`, role
`fabric-coordinator`, `ori claude` through `runtime/openrouter/launch`, preset
`deepseek2claude-shim` (system-prompt-only compatibility delta, prepended).
Prompt for every check: `api status ?` in an interactive session, or the
six-step headless task in §4.

## 1. v4-flash-0731 is not admissible

Three sessions, two shim texts, price-sorted provider (OpenInference every
time). All three broke at the second or third long tool result, prompt
~25–26k tokens:

| session | gens | what broke |
|---|---|---|
| `39c84ae7` | `gen-1789381654-ost80kBKpx3ZXSpxsZP4` … `gen-1789381688-SWvlejufdw48kGh8HRou` (4) | turn 3 opens with the fragment `tokens>15059/>`, then invents a user question ("What does the midje 'structure' do…") and acts on it; turn 4 is 632 tokens of fabricated `<total_tokens>`/`<system-reminder>` markup |
| `5e8079f6` (shim v2: turn discipline + harness markup sections) | `gen-1789382281-XYN3x4OCjsw0q2TT5zAt` … `gen-1789382408-ZN5PrEWGclX9aIQK4QY4` (6) | turn 3 emits a fabricated "courier's dispatch / envelope" as assistant text; turns 4–5 read that text back as a prompt injection by the user and investigate it; reasoning grows 179 → 969 → 1022 → 1623 tokens per turn — a loop on its own output |
| `ee051893` | `gen-1789382718-WexTsBOndfoYOoERB3z0` … `gen-1789382748-KvWCzOIVuSXqIlIEfZoU` (3) | turn 2 writes a user message in Chinese, translates it, treats it as the request; `cd /home/user/project` (nonexistent) |

`native_tokens_cached` on OpenInference is cumulative across the
conversation (14k → 31k → 50k → … against 25–32k prompts): impossible, and
a second reason not to trust that serving. Conclusion: the shim text does
not reach this failure — the model loses the role structure of a long,
tag-dense Claude Code context. No capability class may resolve to it.

## 2. v4-pro-0813 holds

`5cbbca76`, 2 gens (`gen-1789382991-mzVO2EKleBRp37Q7EEgI`,
`gen-1789383004-OjGC5HywULVpPv8U0WMr`), Ionstream: `fabric-status`, then a
correct, well-formed answer; reasoning 229 → 1 tokens; cache read 24 064 on
the second turn. Cost $0.022 + $0.0045.

## 3. The provider pin, and why DeepSeek first-party is out

Preset v6 `order: [deepseek]` with fallbacks on: `d60a1a59` (8 gens,
`gen-1789384784-qC76ipwQhCZr6b2UmF1I` … `gen-1789384885-hUEsY4n2WCqThZ91jXcu`)
was served by **Ionstream on every turn** — the order was silently
skipped. Preset v7 `only: [deepseek]`, fallbacks off: `0d324dd2`, one
request, HTTP 400:

> 0 endpoints out of 1 requested are available matching your guardrail
> restrictions and data policy … Paid model training violation (account
> settings): 1 endpoint excluded; configurable at
> https://openrouter.ai/settings/privacy

DeepSeek's own endpoint trains on paid prompts; this account's privacy
setting excludes such providers, and that setting stays (agents' prompts
carry project code and knowledge). Preset v8 pins `only: [ionstream]`,
fallbacks off. Cost on Ionstream: $0.88 / $2.64 per M in/out, cache read
$0.03/M.

## 4. Six-step check on the pinned preset

Headless: `launch --model deepseek/deepseek-v4-pro-0813@preset/deepseek2claude-shim -p …`,
task: read `CLAUDE.md`, `README.md`, `runtime/openrouter/launch` (16k
chars), `tests/run.sh`, `routing/capabilities.json` in full, run
`bin/fabric-status`, then answer four questions from what was read.

Session `45018d63`, 9 generations, all Ionstream, `deepseek/deepseek-v4-pro-20260813`:

| gen | prompt | completion | reasoning | cached | finish | cost |
|---|---|---|---|---|---|---|
| `gen-1789385248-jVSkWsXU6kfwVGuzEzLJ` | 22 892 | 129 | 119 | 19 968 | stop | $0.0045 |
| `gen-1789385254-YGOpDbVVPG1o6kuDKIxc` | 22 918 | 54 | 38 | 19 968 | tool_calls | $0.0045 |
| `gen-1789385260-MbiiI6fbxH16TFjVcG7n` | 25 323 | 17 | 1 | 25 088 | tool_calls | $0.0025 |
| `gen-1789385262-X1b3ZFzBol4e7Cmi3T2x` | 26 868 | 20 | 1 | 25 088 | tool_calls | $0.0040 |
| `gen-1789385264-qIx40QJGbV27u2cd7OhE` | 31 242 | 17 | 1 | 26 880 | tool_calls | $0.0068 |
| `gen-1789385266-5zJZByytxbnXcFFs6rqK` | 31 964 | 20 | 1 | 31 744 | tool_calls | $0.0038 |
| `gen-1789385277-a1VeCFz3SH6m1TyGIZVS` | 32 738 | 32 | 1 | 32 512 | tool_calls | $0.0039 |
| `gen-1789385284-oejU9LjrLJFMUpu4cIcx` | 32 806 | 29 | 1 | 33 536 | tool_calls | $0.0032 |
| `gen-1789385287-ZZUo2VVdlo4nbEBY28kn` | 33 180 | 206 | 1 | 33 536 | stop | $0.0042 |

Result: the six steps in order, each file read in full, the answer written
after step 6 without prompting; (a) API path and session model, (b) the
four classes and their models, (c) the attribution rule quoted, (d) 15
`run` lines — all correct. Total $0.037 (Claude Code's own
`total_cost_usd` of $0.36 is computed from Anthropic tables and is not
the bill). Cache read tracks the previous prompt, as it should.

Defects, both first-turn: gen 1 ended with `stop` after emitting the
model's native `<｜DSML｜tool_calls>` markup inside reasoning instead of a
tool call (the harness nudged; gen 2 issued it correctly); gen 3 carried
an empty text block beside the tool call (a second nudge). Two nudges in
nine turns, zero hallucinations. The previous, unpinned run (`d60a1a59`)
also executed every step correctly but never stopped to answer — it kept
reading beyond the task until `--resume` asked for the answer; the
"after step 6 read nothing else" line in the task removed that.

## What this decides

`routing/shims.json`: family `deepseek/deepseek-v4*` → `@preset/deepseek2claude-shim`,
with v4-flash recorded as tested and not admissible. Whether v4-pro serves
a class is a separate decision (`routing/policies/review-grade.json`,
architect-cto's).
