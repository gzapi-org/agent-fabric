{
  "description": "gzapi.ge's GZCoord integration: the site's one role, brand-comms, is on the fleet's coordination channel with the agents it works beside (architect-cto, devex-tooling, the decks), hosted by the relay on the coordinator's workspace — one fleet, one channel, whatever repository a session stands in. Read by communication/gzcoord/scripts/inbox.mjs when the working copy resolves to project gzapi.ge; CLAUDE_BRIDGE_URL and GZCOORD_CHANNEL in the environment override it. No token_env_file: the account's token arrives with fabric-secrets sync (its own Doppler config), and a private site repository keeps no env file for it. relay_runtime_dir is relative to the workspace, never inside a repository. Written 2026-09-16, when the account was read back deaf.",
  "relay_url": "http://127.0.0.1:8765",
  "channel": "gzapp:gzcoord",
  "relay_runtime_dir": ".gzcoord"
}
