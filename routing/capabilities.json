{
  "version": 1,
  "description": "CAPABILITY CLASS -> CONCRETE MODEL, per provider. A capability class says how much reasoning a task needs; a provider entry says which model currently satisfies it there. Compatibility shims (routing/shims.json) are a separate dimension keyed on the model family, and the composite `model@preset/slug` string is derived at runtime by tools/fabric/routing.py — it is never written here.",
  "classes": {
    "code-low": {
      "title": "Inexpensive, mechanical delegated coding work",
      "description": "Simple repository exploration, routine modifications, test generation, documentation updates, lint fixes, and other low-risk tasks."
    },
    "code-medium": {
      "title": "Default delegated coding worker",
      "description": "Normal engineering work that needs solid reasoning but is not unusually difficult or high-consequence."
    },
    "code-high": {
      "title": "Difficult or high-consequence engineering work",
      "description": "Architecture, difficult debugging, security review, subtle refactoring, concurrency, protocol design, and important cross-component reasoning."
    },
    "review": {
      "title": "The review class",
      "description": "The adversarial substitute reviewer. Not a coding class: a review's failure mode is a green PR that merges, so its model is gated by routing/policies/review-grade.json and is never a cost decision."
    }
  },
  "providers": {
    "anthropic": {
      "resolution": "harness",
      "description": "Plain `claude`, Anthropic by construction. Values are harness model references: a tier alias (haiku, sonnet, opus) the harness binds to its current Claude model of that tier, or a full model id where a class must not share an alias with another. The review class declares the full id so that, on the broker path, it never rides the same export as code-high. No shim applies to a native Claude model.",
      "models": {
        "code-low": "haiku",
        "code-medium": "sonnet",
        "code-high": "opus",
        "review": "claude-opus-5[1m]"
      }
    },
    "openrouter": {
      "resolution": "model-id",
      "description": "The broker path (runtime/openrouter/launch via the ori CLI). Values are OpenRouter model ids; the launcher derives the composite with the family shim and exports it under the harness alias the class maps to (runtime/claude-code/aliases.json). A class the harness binds by full id rather than by alias (review) is not exported: its agent file names the model and the launcher checks the two agree.",
      "models": {
        "code-low": "z-ai/glm-5.3-flash",
        "code-medium": "z-ai/glm-5.2",
        "code-high": "z-ai/glm-5.3",
        "review": "anthropic/claude-opus-5[1m]"
      }
    }
  }
}
