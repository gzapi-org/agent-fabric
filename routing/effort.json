{
  "version": 1,
  "description": "CAPABILITY CLASS -> REASONING EFFORT. The third dimension, beside CLASS -> MODEL (routing/capabilities.json) and MODEL FAMILY -> SHIM (routing/shims.json), and kept here for the same reason they are kept apart: one file, one decision. How much thinking a class gets is a property of the WORK, so it is one vocabulary here and not a column per provider; how a provider SPELLS it is that provider's adapter (tools/fabric/routing.py), and whether a given model admits a level is the adapter's per-model table. Effort exists as a routed value because it was not one: every model the fabric had pinned happened to default to `high`, and Opus 5.5 was the first that did not — moving code-high to it would have dropped every session a level with no commit and nothing to review (docs/live-checks/2026-09-23-opus-5-5.md). A model's own default is never the fabric's decision.",
  "levels": [
    "none",
    "minimal",
    "low",
    "medium",
    "high",
    "xhigh",
    "max"
  ],
  "levels_note": "Ordered, least to most. The union of the vendors' scales, not any one vendor's: Anthropic admits low..max, OpenAI all seven, xAI none..xhigh, DeepSeek low/high/max, GLM-5.3-Flash low/high/max. A level is an ORDINAL, never a magnitude — no vendor asserts that its `high` means another's, and neither does this file. What a class asks for is clamped DOWN to the nearest level its model admits, by the fabric, before the request leaves: the vendors disagree on the failure mode (OpenAI returns 400, xAI and Claude Code clamp silently, GLM-5.3-Flash errors, DeepSeek remaps), and a routing decision must not mean two different things depending on who serves it.",
  "classes": {
    "code-low": "medium",
    "code-medium": "medium",
    "code-high": "medium",
    "code-plan": "medium",
    "code-review": "medium"
  },
  "session": "medium",
  "session_note": "A level, or a capability class whose level to take — as routing/profiles.json's own `session` may name a class. Every session thinks at medium (the owner, 2026-09-25), as every class does, so the level is named directly. A layer that gives one agent's session another model does not move its level: architect-cto-01's session is Fable 5.1 (routing/profiles.json) and thinks at medium too.",
  "providers": {
    "anthropic": {
      "classes": {},
      "notes": {
        "all": "No override: every class is Opus 5.5 at medium (the owner, 2026-09-25), which Opus 5.5 admits and is its own default — so on plain claude the level is now the vendor's default as well as the fabric's decision. Before, code-low rode Haiku 4.5, which takes no effort (null here), and code-medium asked Sonnet 5 for less than its default high."
      }
    },
    "openrouter": {
      "classes": {
        "code-low": "low"
      },
      "notes": {
        "code-low": "code-low asks for medium (the owner, 2026-09-25: every class at medium); z-ai/glm-5.3-flash distinguishes only low/high/max, and the nearest level at or below medium is low, so low is what it serves. Written down because that is a level lost, and a lost level is a commit.",
        "all": "Every other class asks medium and its broker model has no medium: z-ai/glm-5.2 (code-medium) and deepseek/deepseek-v4-pro-0813 (code-high, code-plan, code-review) are served high, the vendors' documented mapping, one level above what the class asks. Nothing is lost there, so no override. On the broker code-high, code-plan and code-review therefore differ in neither model nor effort. That is true of today's broker column, not a property of the design."
      }
    }
  }
}
