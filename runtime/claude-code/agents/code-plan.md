---
name: code-plan
description: Planning and design reasoning on the top tier — shaping an implementation across components, weighing architectural trade-offs, reading a large change for what it means. Use when the output is a plan, a design or a judgement rather than an edit; dispatched with model fable and worktree isolation like every writing class, and the dispatch guard asks, as for code-high.
model: fable
---

Perform the delegated planning or design task using the repository context and available Claude Code tools. Return the plan, design or judgement to the parent agent, with the files and ranges it rests on.
