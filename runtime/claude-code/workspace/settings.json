{
  "_comment": "Template for <projects>/.claude/settings.json, written by runtime/claude-code/bootstrap.sh with $AGENT_FABRIC_ROOT substituted. Hooks resolve the agent from the OS and show the working copy as context; nothing here names an agent or a project. The GZCoord inbox drains here too: started in the workspace, a session is outside any checkout, and the inbox reads the project and its token from the binding. env disables the harness's own tab-title writer: it writes a prompt-derived topic after every turn, on top of what hooks/tab-title.sh set, so the tab named a conversation instead of the agent (db-admin, 2026-09-15); with it off the hook is the only writer. This scope reaches only a session started from the workspace itself; a session started inside a clone gets the switch from runtime/openrouter/launch, which exports it on every path (2026-09-16).",
  "env": {
    "CLAUDE_CODE_DISABLE_TERMINAL_TITLE": "1"
  },
  "statusLine": {
    "type": "command",
    "command": "bash \"$AGENT_FABRIC_ROOT/runtime/claude-code/hooks/statusline.sh\""
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$AGENT_FABRIC_ROOT/runtime/claude-code/hooks/session-start.sh\"",
            "timeout": 15
          },
          {
            "type": "command",
            "command": "bash \"$AGENT_FABRIC_ROOT/runtime/claude-code/hooks/tab-title.sh\"",
            "timeout": 10
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "f=\"$AGENT_FABRIC_ROOT/communication/gzcoord/scripts/inbox.mjs\"; [ -f \"$f\" ] && node \"$f\"; true",
            "timeout": 60
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$AGENT_FABRIC_ROOT/runtime/claude-code/hooks/tab-title.sh\"",
            "timeout": 10
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Agent",
        "hooks": [
          {
            "type": "command",
            "command": "f=\"$AGENT_FABRIC_ROOT/runtime/claude-code/hooks/agent-dispatch-guard.sh\"; if [ -f \"$f\" ]; then bash \"$f\"; else printf '%s\\n' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"ask\",\"permissionDecisionReason\":\"The dispatch guard script was not found; approve only if you have checked model and isolation yourself.\"}}'; fi",
            "timeout": 10
          }
        ]
      },
      {
        "matcher": "Write|Edit|MultiEdit|NotebookEdit|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "f=\"$AGENT_FABRIC_ROOT/runtime/claude-code/hooks/subagent-clone-guard.sh\"; if [ -f \"$f\" ]; then bash \"$f\"; else printf '%s\\n' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"ask\",\"permissionDecisionReason\":\"The subagent clone guard script was not found; approve only if this tool call is not a subagent writing in the session clone.\"}}'; fi",
            "timeout": 10
          }
        ]
      }
    ],
    "PreModelSwitch": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "f=\"$AGENT_FABRIC_ROOT/runtime/claude-code/hooks/model-switch-guard.sh\"; if [ -f \"$f\" ]; then bash \"$f\"; else printf '%s\\n' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreModelSwitch\",\"permissionDecision\":\"ask\",\"permissionDecisionReason\":\"The model-switch guard script was not found; in a broker session a bare model id runs without its family shim. Approve only if the target is a composite or needs no shim.\"}}'; fi",
            "timeout": 10,
            "statusMessage": "agent-fabric: checking the switch keeps its family shim…"
          }
        ]
      }
    ]
  }
}
