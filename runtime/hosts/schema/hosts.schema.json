{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:agent-fabric:hosts",
  "title": "The hosts the fabric operates, and where each account lives",
  "description": "runtime/hosts/registry.json. A host id is its short hostname. Placement maps a Linux login to the host it was provisioned on; it is where the account is, never who it is or what role it holds.",
  "type": "object",
  "required": [
    "version",
    "hosts",
    "placement"
  ],
  "additionalProperties": false,
  "properties": {
    "version": {
      "const": 1
    },
    "description": {
      "type": "string"
    },
    "hosts": {
      "type": "object",
      "minProperties": 1,
      "propertyNames": {
        "pattern": "^[a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?$"
      },
      "additionalProperties": {
        "type": "object",
        "required": [
          "platform",
          "ssh",
          "operator",
          "fabric"
        ],
        "additionalProperties": false,
        "properties": {
          "platform": {
            "enum": [
              "fedora-qubes",
              "fedora",
              "debian",
              "debian-qubes"
            ]
          },
          "ssh": {
            "type": [
              "string",
              "null"
            ],
            "pattern": "^[A-Za-z0-9._-]+@[A-Za-z0-9._:\\[\\]-]+$|^[A-Za-z0-9._:\\[\\]-]+$",
            "description": "An ssh destination for the operator account ([user@]host), or null: the host this registry is read on, reached directly."
          },
          "operator": {
            "type": "string",
            "pattern": "^[a-z_][a-z0-9_-]*$",
            "description": "The login on that host that holds sudo and the fabric checkout; the ssh destination logs in as it."
          },
          "fabric": {
            "type": "string",
            "description": "The operator's fabric checkout on that host; ~ is the operator's home there."
          },
          "operator_key": {
            "type": "string",
            "pattern": "^ed25519:[A-Za-z0-9+/]{40,64}={0,2}$",
            "description": "The public half of the operator's control-plane signing key (SPKI DER, base64), written by `fabric-ctl keygen`. Every control daemon answers an ACTION (runtime/control/sign.mjs) only when it is signed with this key; a host without one can order no action. Optional: read ops need no key."
          }
        }
      }
    },
    "placement": {
      "type": "object",
      "propertyNames": {
        "pattern": "^[a-z_][a-z0-9_-]*$"
      },
      "additionalProperties": {
        "type": "string",
        "pattern": "^[a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?$"
      }
    }
  }
}
